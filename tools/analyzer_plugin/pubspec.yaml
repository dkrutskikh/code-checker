name: code_checker_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 0.4.4

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  code_checker: ^0.4.4
